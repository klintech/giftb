import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "GiftBridge - Send Gifts Worldwide from Nigeria",
  description:
    "Send beautiful gifts from Nigeria to anywhere in the world. Fast, reliable, and affordable international gift delivery.",
  icons: {
    icon: "/favicon.png", // ← ADD YOUR LOGO HERE
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
